namespace = azur_R_ship

ship_event = {
	id = azur_R_ship.1
	hide_window = yes
	fire_only_once = yes

	is_triggered_only = yes

	trigger = {
		owner = {
			has_country_flag = azur_lane_gangqu_country
			NOT = {
				has_country_flag = AL_R_planet_champion
			}
		}
		has_component = AZUR_R_SHENHUA_L_2
	}

	immediate = {
		owner = {
			set_country_flag = AL_R_planet_champion
			capital_scope = {
				add_deposit = d_azur_R_champion
			}
		}
	}
}